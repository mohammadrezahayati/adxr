'use client'
import Container from '@Component/admin/container/container';
import type { FC } from 'react';

interface mainCategoryPageProps {}

const MainCategoryPage: FC<mainCategoryPageProps> = ({}) => {
                return (<Container>main category</Container>);
}
export default MainCategoryPage;