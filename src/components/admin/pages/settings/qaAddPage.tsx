'use client';
import type { FC } from 'react';
import Container from '@Component/admin/container/container';

interface qaAddPageProps {}

const QaAddPage: FC<qaAddPageProps> = ({}) => {
  return <Container>QA Add</Container>;
};
export default QaAddPage;
